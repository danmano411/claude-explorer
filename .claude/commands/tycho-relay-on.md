---
description: "Turn ON the verdict relay — agent sees non-VERIFIED verdicts, works till VERIFIED (bounded)"
---

<!-- Generated by tycho init. -->

Run this exact command with the Bash tool and show its output verbatim - nothing else, no commentary:

`C:/Users/danma/.local/bin/tycho.EXE relay --on`

This is Tycho's own CLI (it also runs standalone in your terminal); it never edits code.
