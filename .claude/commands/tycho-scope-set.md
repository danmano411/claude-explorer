---
description: "Replace the scope_drift allowlist with these path globs"
argument-hint: "<glob>… e.g. 'src/**' 'tests/**'"
---

<!-- Generated by tycho init. -->

Update this repo's Tycho edit-scope, then show the command's output verbatim. Run it with the Bash tool, passing **each glob as a separate single-quoted argument** so the shell keeps it literal (an unquoted `src/**` expands to files before Tycho stores it):

`C:/Users/danma/.local/bin/tycho.EXE scope set '<glob>' '<glob>' …`

The globs to use: $ARGUMENTS

This edits `.tycho.toml` only — it never touches your code.
