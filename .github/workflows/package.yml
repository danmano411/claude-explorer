name: Package

# Packaging. Runs on a version-tag push (matching release.ps1's `v<version>`
# tag convention) or manual dispatch. `workflow_dispatch` builds but never
# publishes; only a tag push creates a release, and it is a DRAFT — Dan still
# publishes manually (see CLAUDE.md "Release process"). Nothing auto-publishes.
#
# Three platforms: Windows (NSIS), macOS (DMG, one leg per architecture) and
# Linux (AppImage). No leg runs the test suite — that is PR Check's job, on
# every PR into main; duplicating it here would only make releases slower.
on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows:
    name: Windows (NSIS)
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      # `--publish never`, always. electron-builder 26.x's own `--publish
      # always` races itself into duplicate drafts (see release.ps1 and
      # CLAUDE.md's Release process section) — that bug is why release.ps1
      # never uses electron-builder to publish and instead calls `gh
      # release create` itself once, by hand, over already-built files. The
      # publish-draft job below reproduces exactly that, from artifacts
      # collected here.
      - name: Package (NSIS)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx electron-builder --win --publish never

      # electron-builder writes dist/latest.yml referencing the HYPHENATED
      # asset name ("Claude-Explorer-Setup-<version>.exe") because GitHub
      # mangles spaces in release asset filenames, but the NSIS installer it
      # actually produces is named with spaces
      # ("Claude Explorer Setup <version>.exe"). release.ps1 copies the
      # built files to the hyphenated names before uploading so the asset
      # electron-updater fetches matches what latest.yml points at — mirror
      # that exactly here.
      - name: Rename installer assets to match latest.yml
        shell: pwsh
        run: |
          $version = (Get-Content package.json -Raw | ConvertFrom-Json).version
          $dist = Join-Path $PWD 'dist'
          $exe = Join-Path $dist "Claude-Explorer-Setup-$version.exe"
          Copy-Item (Join-Path $dist "Claude Explorer Setup $version.exe") $exe -Force
          Copy-Item (Join-Path $dist "Claude Explorer Setup $version.exe.blockmap") "$exe.blockmap" -Force

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: |
            dist/Claude-Explorer-Setup-*.exe
            dist/Claude-Explorer-Setup-*.exe.blockmap
            dist/latest.yml
          if-no-files-found: error

  # KAN-92. No Developer ID, by owner decision — no Apple Developer Program
  # membership ($99/yr), so no notarization and NO SIGNING SECRETS ANYWHERE IN
  # THIS FILE. electron-builder's own signing step still logs "skipped macOS
  # application code signing ... 0 identities found".
  #
  # But the bundle IS ad-hoc signed, by build/adhoc-sign.cjs (afterPack, PR
  # #83), and that is not the consolation prize KAN-92 turned down. Measured on
  # a macos-26 arm64 runner against the shipped v0.10.0 DMG: with no signing
  # step at all the bundle kept only Electron's per-binary linker signatures,
  # `Sealed Resources=none`, and Gatekeeper judged a quarantined copy INVALID
  # ("code has no resources but signature indicates they must be present") —
  # the "is damaged" / silent-hang path. Ad-hoc signing seals 181 resources and
  # Gatekeeper's verdict becomes a plain "rejected": valid but unnotarized,
  # which is macOS's normal Open Anyway path.
  #
  # KAN-92's real objection was hardened runtime, and it still stands: it
  # would kill Electron's V8 JIT without allow-jit entitlements.
  # electron-builder's `mac.identity: "-"` would turn it on; the hook calls
  # codesign WITHOUT `--options runtime`, so the signature is `flags=0x2(adhoc)`
  # and the app launches with all its helpers. Keep it that way.
  #
  # Auto-update is disabled on macOS in src/main/updater.ts (Squirrel.Mac
  # verifies a Developer ID signature that does not exist), so this leg
  # deliberately builds DMG ONLY. electron-builder's usual advice is to ship a
  # zip alongside the DMG, but a zip's only job is to feed electron-updater —
  # with no update path it is a second ~100 MB asset nobody can use, and the
  # DMG is what a Mac user expects to download anyway. Add `zip` to
  # build.mac.target, and latest-mac.yml to publish-draft, the day signing lands.
  #
  # One leg per architecture rather than a universal binary, because
  # @vscode/ripgrep ships per-arch optional dependencies: `npm ci` on an
  # arm64 runner installs @vscode/ripgrep-darwin-arm64 and NOT -darwin-x64,
  # so a cross-built x64 package would fail to find its extraResources.
  # node-pty is fine either way — it vendors every prebuild in one package.
  macos:
    name: macOS (DMG, ${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      # One arch failing must not cancel the other; they are separate
      # deliverables and a broken Intel leg should not hide a good arm64 DMG.
      fail-fast: false
      matrix:
        include:
          # `macho` is what `file(1)` calls the same architecture — the
          # verify step below compares against it.
          - runner: macos-latest
            arch: arm64
            macho: arm64
          - runner: macos-15-intel
            arch: x64
            macho: x86_64
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Package (DMG)
        run: npx electron-builder --mac --${{ matrix.arch }} --publish never

      # KAN-92 acceptance criterion 3: search must find the darwin ripgrep in
      # the packaged app. This is the one behavioural criterion a runner can
      # actually settle, and it is worth settling — `extraResources` points at
      # @vscode/ripgrep-darwin-${arch}, a PER-ARCH optional dependency, so the
      # failure mode is a package that builds clean and then degrades every
      # search to filename-only forever (resolveRg() returns null by design
      # rather than throwing). Running `rg --version` is the assertion: a
      # wrong-architecture binary copies in perfectly and only fails to execute.
      - name: Verify packaged resources
        run: |
          set -euo pipefail
          app=$(ls -d dist/mac*/'Claude Explorer.app')
          echo "bundle: $app"
          file "$app/Contents/MacOS/Claude Explorer"
          file "$app/Contents/MacOS/Claude Explorer" | grep -q '${{ matrix.macho }}'
          file "$app/Contents/Resources/rg"
          file "$app/Contents/Resources/rg" | grep -q '${{ matrix.macho }}'
          "$app/Contents/Resources/rg" --version

      # PR #83. Every released DMG through 0.10.1 could not open a single
      # terminal or Claude tab: node-pty 1.1.0 ships prebuilds/darwin-*/
      # spawn-helper as 0644, nothing chmods it, and posix_spawn refuses a
      # non-executable helper ("posix_spawnp failed."). build/fix-node-pty.cjs
      # is the fix; this is the assertion. Like `rg --version` above, the bug
      # only shows when something executes, so it spawns a real pty through the
      # packaged app's own Electron and node-pty.
      - name: Verify a terminal can start
        run: |
          set -euo pipefail
          app="$PWD/$(ls -d dist/mac*/'Claude Explorer.app')"
          ELECTRON_RUN_AS_NODE=1 "$app/Contents/MacOS/Claude Explorer" build/verify-pty.cjs \
            "$app/Contents/Resources/app.asar/node_modules/node-pty"

      # Only the DMG. latest-mac.yml is generated but deliberately NOT
      # uploaded: publishing an update feed for a platform whose updater is
      # switched off is how a user ends up with a download that cannot install.
      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-dmg-${{ matrix.arch }}
          path: dist/Claude-Explorer-*.dmg
          if-no-files-found: error

  # KAN-93. AppImage only: the one Linux artifact electron-updater can update,
  # and it needs no package manager. deb/rpm users update through theirs.
  linux:
    name: Linux (AppImage)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # BEFORE npm ci, and installed explicitly rather than assumed present.
      # node-pty's prebuilds/ has darwin-arm64, darwin-x64, win32-arm64 and
      # win32-x64 — and NOTHING for Linux — so its install script falls back to
      # `node-gyp rebuild`, which needs a C++ toolchain and python3. Trusting
      # the runner image for this is exactly what breaks silently the next time
      # GitHub rotates it.
      - name: Install node-pty build prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      # npmRebuild stays false even here: node-pty is N-API, so the binary
      # node-gyp just produced above loads in Electron unchanged.
      - name: Package (AppImage)
        run: npx electron-builder --linux --publish never

      # KAN-93 acceptance criteria 1 and 3, checked against the artifact that
      # actually ships rather than against dist/linux-unpacked. --appimage-extract
      # is the AppImage runtime's own flag and needs no FUSE, so it works on a
      # headless runner. Running `rg --version` is the real assertion: a
      # wrong-platform binary copies in fine and only fails to execute.
      - name: Verify packaged AppImage
        run: |
          set -euo pipefail
          appimage=$(ls dist/Claude-Explorer-*.AppImage)
          echo "artifact: $appimage"
          "$appimage" --appimage-extract >/dev/null
          echo '--- generated .desktop entry ---'
          cat squashfs-root/*.desktop
          echo '--- executable name (this is what Chromium reports as WM_CLASS) ---'
          ls squashfs-root/claude-explorer
          file squashfs-root/resources/rg
          file squashfs-root/resources/rg | grep -q 'ELF 64-bit'
          squashfs-root/resources/rg --version
          # StartupWMClass only does its job if it matches the executable's own
          # name; asserting that beats trusting the string in package.json.
          grep -qx 'StartupWMClass=claude-explorer' squashfs-root/*.desktop
          grep -qx 'Categories=Development;' squashfs-root/*.desktop

      # latest-linux.yml IS uploaded — unlike macOS, AppImage auto-update
      # works, so electron-updater needs its feed in the release.
      - name: Upload AppImage artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-appimage
          path: |
            dist/Claude-Explorer-*.AppImage
            dist/latest-linux.yml
          if-no-files-found: error

  publish-draft:
    name: Publish draft release
    needs: [windows, macos, linux]
    # Only a tag push publishes anything; `workflow_dispatch` builds/tests
    # every leg and stops (artifacts stay attached to the run for
    # inspection). `success()` must be explicit: adding a custom `if:` to a
    # job replaces the default "all `needs` succeeded" gate rather than
    # adding to it.
    if: success() && startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Windows installer
        uses: actions/download-artifact@v4
        with:
          name: windows-installer
          path: dist

      # Two mac legs, one artifact each; merge-multiple flattens them into the
      # same dist/ since their filenames already carry the architecture.
      - name: Download macOS DMGs
        uses: actions/download-artifact@v4
        with:
          pattern: macos-dmg-*
          merge-multiple: true
          path: dist

      - name: Download Linux AppImage
        uses: actions/download-artifact@v4
        with:
          name: linux-appimage
          path: dist

      # Update feeds: latest.yml (Windows) and latest-linux.yml (AppImage).
      # There is no latest-mac.yml on purpose — see the macos job's comment.
      - name: Create draft release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            --draft --title "${{ github.ref_name }}" --generate-notes \
            dist/Claude-Explorer-Setup-*.exe \
            dist/Claude-Explorer-Setup-*.exe.blockmap \
            dist/latest.yml \
            dist/Claude-Explorer-*.dmg \
            dist/Claude-Explorer-*.AppImage \
            dist/latest-linux.yml
