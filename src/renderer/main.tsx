// Phase 0 placeholder — replaced by Task 3.1 (mounts <App/> + index.css).
import { createRoot } from 'react-dom/client'

createRoot(document.getElementById('root')!).render(
  <div style={{ fontFamily: 'serif', padding: 24, color: '#2B2622', background: '#F5F1E8' }}>
    Claude Explorer — scaffold OK
  </div>,
)
